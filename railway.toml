[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 inject_meta.py && streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
